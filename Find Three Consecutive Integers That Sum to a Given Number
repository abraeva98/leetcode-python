class Solution:
    def sumOfThree(self, num: int) -> List[int]:
        if num % 3 != 0:
            return []
        divisibleByThree = math.floor(num / 3)
        return [divisibleByThree - 1, divisibleByThree, divisibleByThree + 1]
