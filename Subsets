class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        res = [[]]
        
        for num in nums:
            temp = []
            for subset in res:
                j = [[num] + subset]
                temp += j
            res += temp
        return res
        
